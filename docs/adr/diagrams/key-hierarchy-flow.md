# Key Hierarchy Flow Diagram

## Visual Overview

This diagram illustrates the complete key hierarchy from ADR-0002.

```
┌──────────────────────────────────────────────────────────────────────────┐
│ USER INPUT LAYER                                                          │
│                                                                           │
│ ┌────────────────┐         ┌───────────────┐                            │
│ │ User Password  │         │ Random Salt   │                            │
│ │ (sensitive)    │         │ (32 bytes)    │                            │
│ └────────┬───────┘         └───────┬───────┘                            │
│          │                         │                                     │
│          └─────────────┬───────────┘                                     │
│                        │                                                 │
└────────────────────────┼─────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ TIER 1: MASTER KEY (password-derived)                                    │
│                                                                           │
│            PBKDF2-HMAC-SHA256 (100,000 iterations)                       │
│                        │                                                 │
│                        ▼                                                 │
│              ┌────────────────────┐                                      │
│              │ User Master Key    │  ◄───── Stored in iOS Keychain      │
│              │ (256-bit, AES-256) │         kSecAttrAccessibleWhen      │
│              └──────────┬─────────┘         UnlockedThisDeviceOnly      │
│                         │                   Never synced / transmitted   │
│                         │                                                │
└─────────────────────────┼────────────────────────────────────────────────┘
                          │
         ┌────────────────┴────────────────┐
         │                                 │
         ▼                                 ▼
┌──────────────────────────┐     ┌─────────────────────────┐
│ Encrypts User Identity   │     │ Wraps Owner's FMKs      │
└────────────┬─────────────┘     └────────────┬────────────┘
             │                                │
             ▼                                │
┌──────────────────────────────────────────────────────────────────────────┐
│ TIER 2: USER IDENTITY (for sharing, not password-derived)                │
│                                                                           │
│        Curve25519.KeyAgreement.PrivateKey (generated randomly)           │
│                        │                                                 │
│        ┌───────────────┴───────────────┐                                │
│        │                               │                                │
│        ▼                               ▼                                │
│  ┌─────────────────┐         ┌──────────────────┐                      │
│  │ Private Key     │         │ Public Key       │                      │
│  │ (encrypted      │         │ (shareable)      │                      │
│  │  with Master    │         │                  │                      │
│  │  Key)           │         │                  │                      │
│  └────────┬────────┘         └──────────┬───────┘                      │
│           │                             │                               │
│    Stored in Keychain          Stored in Core Data + Server            │
│    kSecAttrAccessible          (plaintext - it's public!)              │
│    WhenUnlockedThisDeviceOnly                                          │
│           │                             │                               │
└───────────┼─────────────────────────────┼───────────────────────────────┘
            │                             │
            │ (used for ECDH)             │ (exchanged with other users)
            │                             │
            │    ┌────────────────────────┘
            │    │
            ▼    ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ TIER 3: FAMILY MEMBER KEYS (per-patient data encryption)                 │
│                                                                           │
│     For each family member (Emma, Liam, etc.):                           │
│                                                                           │
│     SymmetricKey.init(size: .bits256)  ◄─── Generated randomly           │
│                 │                            (NOT password-derived)      │
│                 ▼                                                        │
│     ┌────────────────────────┐                                          │
│     │ Family Member Key (FMK)│                                          │
│     │ for Emma               │                                          │
│     └────────┬───────────────┘                                          │
│              │                                                           │
│              │ Wrapped in two ways:                                     │
│              │                                                           │
│       ┌──────┴───────┐                                                  │
│       │              │                                                  │
│       ▼              ▼                                                  │
│  ┌──────────┐  ┌──────────────────┐                                    │
│  │ Owner's  │  │ Shared Users'    │                                    │
│  │ Copy     │  │ Copies           │                                    │
│  └────┬─────┘  └──────┬───────────┘                                    │
│       │               │                                                 │
│       │               │                                                 │
│  Wrapped with    Wrapped with ECDH-derived key:                        │
│  Master Key      - ECDH(My Private Key, Their Public Key)              │
│       │          - HKDF(Shared Secret, context)                        │
│       │          - AES.KeyWrap(FMK, wrapping key)                      │
│       │               │                                                 │
│  Stored in       Stored in Core Data                                   │
│  Keychain        (synced to other users)                               │
│       │               │                                                 │
└───────┼───────────────┼─────────────────────────────────────────────────┘
        │               │
        └───────┬───────┘
                │
                ▼
┌──────────────────────────────────────────────────────────────────────────┐
│ TIER 4: MEDICAL RECORDS (encrypted data)                                 │
│                                                                           │
│     All records for Emma:                                                │
│     ┌──────────────────────────────────────────┐                        │
│     │ - Vaccine: COVID-19, Pfizer, 2025-01-10  │                        │
│     │ - Allergy: Peanuts (severe)              │  Encrypted with        │
│     │ - Medication: Amoxicillin 250mg          │  Emma's FMK            │
│     │ - Appointment: Dr. Smith, 2025-02-15     │  using AES-256-GCM     │
│     └──────────────────────────────────────────┘                        │
│                        │                                                 │
│                        ▼                                                 │
│     ┌──────────────────────────────────────────┐                        │
│     │ Encrypted Blobs (stored in Core Data)    │                        │
│     │ - Synced to server (zero-knowledge)      │                        │
│     │ - Server cannot decrypt (no FMK)         │                        │
│     └──────────────────────────────────────────┘                        │
│                                                                           │
└──────────────────────────────────────────────────────────────────────────┘
```

## Sharing Flow Example

```
Adult A shares Emma's records with Adult B:

┌──────────────────────┐
│ Adult A              │
│                      │
│ 1. Initiates sharing │
│    via email         │
└──────────┬───────────┘
           │
           │ Email invitation
           ▼
┌──────────────────────┐
│ Adult B              │
│                      │
│ 2. Accepts invite    │
│ 3. Public Key sent   │
│    to server         │
└──────────┬───────────┘
           │
           │ Adult B's Public Key
           ▼
┌──────────────────────────────────────┐
│ Adult A's Device                      │
│                                      │
│ 4. Retrieve Adult B's Public Key     │
│ 5. Unwrap Emma's FMK (from Keychain) │
│    using Master Key                  │
│ 6. Perform ECDH:                     │
│    - Shared Secret =                 │
│      KeyAgreement(My Private Key,    │
│                   B's Public Key)    │
│    - Wrapping Key = HKDF(Shared      │
│                      Secret)         │
│ 7. Wrap Emma's FMK with Wrapping Key │
│ 8. Store wrapped FMK in Core Data    │
│ 9. Sync to server                    │
└──────────┬───────────────────────────┘
           │
           │ Wrapped FMK (for Adult B)
           ▼
┌──────────────────────────────────────┐
│ Server (zero-knowledge)               │
│                                      │
│ - Stores wrapped FMK (encrypted)     │
│ - Cannot decrypt (no private keys)   │
└──────────┬───────────────────────────┘
           │
           │ Download wrapped FMK
           ▼
┌──────────────────────────────────────┐
│ Adult B's Device                      │
│                                      │
│ 10. Retrieve Adult A's Public Key    │
│ 11. Perform ECDH:                    │
│     - Shared Secret =                │
│       KeyAgreement(My Private Key,   │
│                    A's Public Key)   │
│     - Wrapping Key = HKDF(Shared     │
│                      Secret)         │
│ 12. Unwrap Emma's FMK                │
│ 13. Decrypt Emma's records           │
│     using FMK                        │
└──────────────────────────────────────┘

Result: Adult B can now decrypt all of Emma's medical records
```

## Key Rotation Flow (Access Revocation)

```
Adult A revokes Adult C's access to Emma's records:

┌──────────────────────────────────────┐
│ Step 1: Generate new FMK_Emma        │
│                                      │
│ SymmetricKey.init(size: .bits256)    │
│          ↓                           │
│    New FMK_Emma (256-bit)            │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│ Step 2: Re-encrypt all Emma's records│
│                                      │
│ For each record:                     │
│   - Decrypt with old FMK_Emma        │
│   - Re-encrypt with new FMK_Emma     │
│                                      │
│ Performance: ~500ms for 500 records  │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│ Step 3: Re-wrap new FMK for          │
│         authorized users only        │
│                                      │
│ - Wrap for Adult A (Master Key)      │
│ - Wrap for Adult B (ECDH)            │
│ - DELETE Adult C's wrapped FMK       │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│ Step 4: Sync changes                 │
│                                      │
│ - New encrypted records → server     │
│ - New wrapped FMKs → server          │
│ - Adult C's wrapped FMK deleted      │
└──────────┬───────────────────────────┘
           │
           ▼
┌──────────────────────────────────────┐
│ Result:                              │
│                                      │
│ ✅ Adult A: Can decrypt (new FMK)    │
│ ✅ Adult B: Can decrypt (new FMK)    │
│ ❌ Adult C: CANNOT decrypt           │
│    - No wrapped FMK available        │
│    - Old FMK is useless (records     │
│      re-encrypted)                   │
└──────────────────────────────────────┘
```

## Storage Locations Summary

| Key Type | Storage | Protection | Synced? |
|----------|---------|------------|---------|
| User Password | Never stored | N/A | Never |
| Salt | UserDefaults | None (not secret) | No |
| Master Key | Keychain | `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` | Never |
| User Private Key (encrypted) | Keychain | `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` | Never |
| User Public Key | Core Data + Server | None (public) | Yes |
| FMK (owner's copy) | Keychain (wrapped with Master Key) | `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` | No |
| FMK (shared copies) | Core Data (wrapped with ECDH) | Encrypted | Yes |
| Medical Records | Core Data (encrypted with FMK) | Encrypted with AES-256-GCM | Yes |

## Security Properties

### Zero-Knowledge Server

The server stores:

- ✅ User Public Keys (public, not sensitive)
- ✅ Wrapped FMKs (encrypted, server cannot unwrap)
- ✅ Encrypted medical records (encrypted with FMKs, server cannot decrypt)

The server does NOT have:

- ❌ User passwords
- ❌ Master Keys
- ❌ Private Keys
- ❌ Unwrapped FMKs
- ❌ Plaintext medical data

**Result**: Server compromise does not expose medical data (zero-knowledge).

### Device Security

If device is stolen:

- 🔒 **Locked**: All keys in Keychain are inaccessible (Secure Enclave protection)
- 🔓 **Unlocked**: Keychain keys accessible, but require biometric/password to decrypt private key (future enhancement)

### Cryptographic Revocation

Traditional "soft delete" (hide in UI):

- ❌ Revoked user still has FMK
- ❌ Can still decrypt if they saved data locally

Our approach (re-encryption):

- ✅ Revoked user's FMK is deleted
- ✅ All records re-encrypted with new FMK
- ✅ Cryptographically enforced (not just UI hiding)

---

**See also**: ADR-0002 (Key Hierarchy and Derivation)
